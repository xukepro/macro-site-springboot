<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuke.macrosite.dao.LikeDao">

<!--    <resultMap id="likeArticle" type="ArticleDetail" autoMapping="true">-->

<!--    </resultMap>-->
    <insert id="addLike">
        insert into `like` (uid, aid) values (#{uid}, #{aid})
    </insert>

    <delete id="cancelLike">
        delete from `like` where uid = #{uid} and aid = #{aid}
    </delete>

<!--    <select id="getMyLikeArticle" resultType="com.xuke.macrosite.entity.Article">-->
<!--        select a.id,-->
<!--               a.title,-->
<!--               u.nickname author-->

<!--        from `like` l,-->
<!--             article a,-->
<!--             user u-->
<!--        where l.aid = a.id-->
<!--          and a.uid = u.id-->
<!--          and l.uid = 1-->
<!--    </select>-->

<!--    <select id="getAllLikeArticle" resultType="com.xuke.macrosite.entity.Article">-->

<!--    </select>-->
</mapper>